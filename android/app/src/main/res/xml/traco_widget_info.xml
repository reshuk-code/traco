<?xml version="1.0" encoding="utf-8"?>
<!--
  30 minutes is the floor Android enforces for updatePeriodMillis; anything
  smaller is silently rounded up. The widget also refreshes when tapped, which
  is what makes it feel live.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_traco"
    android:configure="app.vercel.traco_pi.twa.widget.TracoWidgetConfigureActivity"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_traco" />
